package com.atfotiad.fakestorechallenge.data.model

/**
 * [LoginResponse] is a data class that represents a login response.
 * @property token is the token of the user.
 * */
data class LoginResponse(
    val token: String
)